import { Component } from '@angular/core';

@Component({
  selector: 'app-home',
  imports: [],
  templateUrl: './home.html'
})
export class HomeComponent {
  // Componente estático, el buscador se movió a la sección de habitaciones
}
